---
version: 5
defaults:
  datadir: /etc/puppetlabs/code/data
  data_hash: yaml_data
hierarchy:
  - name: Node Data
    path: "nodes/%{hostname}.yaml"

  - name: OS Data
    path: "os/%{os.family}"

  - name: Common data
    path: common.yaml
